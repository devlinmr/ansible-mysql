---

###################################################################################################

dependencies:
  - { role: common, consumer_vars: "roles/mysql/vars/{{ ansible_os_family }}" }
  - { role: keepalived, keepalived_enabled: '{{ keepalived_mysql }}' }

###################################################################################################
